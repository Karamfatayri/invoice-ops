version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - invoice-data:/app/data
    env_file: .env
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  invoice-data:
